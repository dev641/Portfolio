import {image, name, designation, socialConnects, summary, email, phone} from '../../data/Contact/ContactDetails.json'
import {type ContactDetails} from '../../types/cards'

const contactDetails: ContactDetails = {
    image,
    name,
    designation,
    summary,
    email,
    phone,
    socialConnects
}

export default contactDetails